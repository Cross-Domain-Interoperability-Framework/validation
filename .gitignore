/ShaclValidation/.idea
node_modules/
framed*.json
new *.txt
.claude/
__pycache__/
_remote_cache/

# Assembled SHACL bundle cached by build_corpus.py --shacl; rebuilt on
# demand from metadataBuildingBlocks, so it is derived, not source.
converters/DCAT/.shacl-coreDiscovery.ttl
converters/DDI/.shacl-CoreDiscovery.ttl

# A .pyc outside __pycache__/ (e.g. carried along in a file move)
# is not caught by the rule above.
*.pyc
